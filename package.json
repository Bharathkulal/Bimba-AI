{
  "name": "bimba-ai-root",
  "version": "1.0.0",
  "description": "Root runner for Bimba AI",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && venv\\Scripts\\uvicorn app.main:app --reload",
    "dev": "npx -y concurrently --kill-others -n \"frontend,backend\" -c \"cyan,green\" \"npm run frontend\" \"npm run backend\""
  }
}
